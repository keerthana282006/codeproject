class Solution {
    public int[] sortedSquares(int[] nums) {
        int n=nums.length;
        int [] num=new int[n];
        for(int i=0;i<n;i++){
            num[i]=nums[i] * nums[i];
        }
        Arrays.sort(num);
        return num;
    }
}
