class Solution {
    public int reverseDegree(String s) {
        int n=s.length();
        
        int ans=0;
        for(int i = 0; i < n; i++) {
            char c = s.charAt(i);
            int val = c - 'a' + 1;
            ans+=(27-val)*(i+1);
        }
        return ans;

        
    }
}
